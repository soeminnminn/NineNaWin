<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_background" >
    
    <android.support.v4.view.ViewPager
	    android:id="@+id/detailViewPager"
	    android:layout_width="match_parent"
	    android:layout_height="match_parent" />
    
    <!-- 
    <android.support.v4.view.ViewPager
	    android:id="@+id/detailViewPager"
	    android:layout_width="match_parent"
	    android:layout_height="match_parent"
	    android:layout_above="@+id/detailPlayerFrame" />
    
    <include layout="@layout/layout_detail_palyer"
        android:id="@+id/detailPlayerFrame"
        android:layout_width="match_parent"
        android:layout_height="@dimen/static_bottombar_height"
        android:layout_alignParentBottom="true" />
     -->
    <ImageButton
        android:id="@+id/action_counter"
        android:layout_width="@dimen/fab_button_size"
        android:layout_height="@dimen/fab_button_size"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="@dimen/fab_button_vertical_margin"
        android:layout_marginRight="@dimen/fab_button_horizontal_margin"
        android:layout_marginEnd="@dimen/fab_button_horizontal_margin"
        android:background="@drawable/fab_button_background"
        android:contentDescription="@string/empty_string"
        android:src="@drawable/ic_patee" />
    
</RelativeLayout>


